import {Image, ImageProps} from 'expo-image';
// import {Platform} from 'react-native';

// const cachePolicy: ImageProps['cachePolicy'] = Platform.select({ios: 'disk', default: 'disk'});

export const ExpoImage = Image;
